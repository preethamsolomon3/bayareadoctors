<%= form_with(model: availability_period, local: true) do |form| %>
  <% if availability_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(availability_period.errors.count, "error") %> prohibited this availability_period from being saved:</h2>

      <ul>
      <% availability_period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
